dist: trusty
language: c
sudo: false

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
      - g++-4.9

before_install:
  - git clone https://github.com/rdaly525/coreir.git
  - export COREIRCONFIG="g++-4.9";
  - export LD_LIBRARY_PATH=`pwd`/coreir/lib:$LD_LIBRARY_PATH
  - cd coreir
  - make -j2 install
  - cd -
  - make -j2 install

script:
  - make test

compiler:
  - gcc

os:
  - linux
